## 2026-09-19 — 520aj 同步脚本改造（引用主站地址）

- 改造 520aj/update_gallery.py：从「扫本地 images 生成数据」改为「引用主站 aj5.netlify.app 地址同步」
  - fetch 主站 manifest.json 生成本地 manifest.json 快照（图片地址全部为 https://aj5.netlify.app/images/...，即引用主站）
  - 复制主站 index.html 的 AUTO_NAV 导航区到 520aj（分类与主站一致）
  - 修复离线回退 bug：原 catch 里 shoes=[]; initializeGallery(); 中 initializeGallery 未定义会白屏，改为主站不可用时 fetch 本地 manifest.json
- 验证：本地快照 298 件，AUTO_NAV_START/END 标记完整，线上仍优先实时 fetch 主站
- 提交推送 520aj（分支 master，非 main）→ 4a14630，Netlify 自动部署
- 易错点：520aj 仓库默认分支是 master（主站是 main），push 用 git push origin master

## 2026-09-19（下）— 主站以 Desktop/图库 为图片源重构导航

- 需求：按 Desktop\图库\ 的 19 个平铺分类（共 336 张）做网站导航标记
- 执行：
  - 旧品牌目录(adidas/gucci/lv/nike/puma/wans/亚瑟) 移入 images_archive/（磁盘保留可恢复，写入 .gitignore 不部署不入库）
  - 复制 Desktop\图库\ 19 分类 → images/（336 张）
  - 改 update_gallery.py：平铺分类原样用文件夹名做导航/分类（去掉 capitalize/upper）
  - 重新生成 AUTO_NAV（19 个独立按钮，无下拉）+ manifest(336 件完整 URL)
- 易错点：移动旧目录后必须 `git add -A` 把旧路径从 git 索引移除，再跑 update_gallery.py；否则脚本的 collect_git_images（git 补录中文目录黑洞逻辑）会把已删的旧 images/ 路径当新图补回，导致 629 件重复
- push 遇 github-actions bot 自动提交(ade07d1, 仅改 index.html 298件旧结构)冲突 → pull --no-rebase + checkout --ours index.html 保留 19 分类版 → 推送 2d2b90d 成功
- 520aj 实时拉主站 manifest，自动同步为 19 分类 336 件

## 2026-09-19 (晚) — 修复 520aj 首屏打开慢

- 根因：520aj 首屏 `loadShoesFromMainSite()` 顶层 `await fetch('https://aj5.netlify.app/manifest.json')` 跨域阻塞，主站响应前白屏等待（贵州→Netlify 跨网尤甚）
- 修复：首屏改为先读同域本地 manifest.json 快照（秒出），后台静默 fetch 主站更新（数据更多才覆盖重渲染当前分类）；head 加 preconnect+dns-prefetch 到主站
- 验证：本地快照 336 件、preconnect 已写入、无冲突标记；推送 520aj master → 1b7acce，Netlify 部署
- 主站 aj5：336 件数据内联在 HTML，首屏不依赖网络，慢只可能 Netlify 边缘冷启/跨网，代码层无根治；如需可后续做"首屏仅内联前40张、其余走 manifest 懒加载"
