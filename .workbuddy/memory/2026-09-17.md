# 2026-09-17 工作日志

## 同步本地图片到网站（aj5.netlify.app）
- 运行 `update_gallery.py`：本地无新增图片，仍 **243 件**（磁盘 264 含 adidas 重复 21 张）。
- github-actions[bot] 又自动同步过（commit `244a26a`，2026-08-25 09:26，同为 243 件）。本次 `git pull --no-rebase` **自动合并成功、无冲突**（两边数据一致，git 直接合并 index.html）。
- 推送成功，远程 `main = d45134f`（= 本地 HEAD），已同步部署。

## ⚠️ 远程状态验证方式
- 本环境 `git status` 的 ahead/behind 可能因 local remote-tracking ref 陈旧误报（本次误报 ahead 41）。**以 `git ls-remote origin main` 为准**确认真实远程 HEAD，勿被 status 误导。

## 环境注意（重要）
- 本机 Git Bash 的 `find`/`wc`/`tail`/`head`/`dirname` 等 coreutils **不可用**（shim `shell-runtime-bash-env.sh` 失败，Git 的 usr/bin 未进 PATH）。改用 `python3` 做文件计数/管道处理；`git` 与 `python3` 本体可用。

## 待办
- `images/adidas/阿迪/` 重复目录仍未删（用户 8-25 选择保留），sync 只认 243 件，磁盘 264 属正常现象。

---

## 第二次同步（17:03 起）
- 本次本地有真实内容变动：nike 乔一/空军换图并新增乔四/小空军/高帮子分类，磁盘图片 264→298。
- `update_gallery.py` 加固：新增 `collect_git_images()` + `merge_git_supplement()`，**从 git 索引（diff --cached + ls-files + others 三源）补录本地 os.walk/Windows API 因中文目录名黑洞而漏扫的图片**，以后同类问题自愈。
- 扫描结果：os.walk 收 277 件 + git 补充 `adidas/阿迪/`(德训/潮流/贝壳头) 21 张重复目录 = **298 件**（与磁盘总数完全一致，符合用户「保留重复目录」意愿）。
- ⚠️ **篮球 5 张图丢失**：git status 曾显示 `A images/篮球/...`，实为 intent-to-add 虚标记（blob 从未入库），工作树 Windows API 也从未真实存在该中文目录；经数次 git add/restore 尝试后 index 条目被清理，git 三源(status/ls-files/diff)现已全部无篮球、cat-file 返回 128，**无法恢复**。需老板重新放入篮球图。
- 推送 `d45134f..016bfa7`，`ls-remote` 核实远程 main == 本地 HEAD，已同步部署。
