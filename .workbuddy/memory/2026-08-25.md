# 2026-08-25 工作日志

## 同步本地图片到网站（aj5.netlify.app）
- 运行 `update_gallery.py`：本地无新增图片，manifest 仍为 **243 件**（磁盘 264 = 243 有效 + 21 重复）。
- 推送时遇到与 `github-actions[bot]` 自动同步提交 `6212357`(2026-06-06 "auto: update gallery") 的 `index.html` 冲突：
  - 该 bot 也会跑脚本自动重写 shoes/nav 区，与本地分页/下拉修复冲突。
  - 对比：本地 HEAD 243 件 > 远程 bot 223 件，且两边分页/下拉修复都在 → **冲突用 `git checkout --ours` 保留本地**，merge 后 push 成功（`6212357..f787f11`）。
- 教训：github-actions bot 会持续自动 update gallery，后续 `git pull` 极可能再冲突，**统一用 ours（本地更全）** 解决即可。

## 待办 / 已知问题
- `images/adidas/阿迪/` 是 `images/adidas/` 根目录 德训/潮流/贝壳头 的**完全重复**（md5 一致，21 张，仅贝壳头有 1 张改名不改内容）。
- 用户本次选择**暂不删除**该重复目录，保留。故 sync 仍只认 243 件。
- 如需彻底消除磁盘 264 vs manifest 243 的偏差，删除 `images/adidas/阿迪/` 即可（破坏性，需用户确认）。
