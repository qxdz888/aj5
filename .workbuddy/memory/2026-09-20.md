
## 2026-09-20 — 主站 aj5 首屏提速（数据分层）

- 问题：主站打开慢。排查确认图片不慢（平均 63KB、已 lazy、缓存头正常），瓶颈在 HTML 93KB 中内联了全部 336 件商品数据
- 方案：数据分层 —— 首屏只内联少量数据，全量后台从 manifest.json 加载
  - update_gallery.py 新增 build_inline()：每分类前 3 张（保证切任意分类都有内容）+ 按序补足到 INLINE_TOTAL=80
  - 新增 LAZY_BLOCK（AUTO_LAZY_START/END 标记包裹，幂等注入）：后台 fetch manifest.json，按 id 去重 push 进 const shoes（const 数组可 push），并把绝对 URL 前缀剥成相对路径
    - 合并后：有搜索词→performSearch 重搜；分类视图→renderGallery 重渲染；全部视图→因 filteredShoes 与 shoes 同引用，仅刷新"加载更多"按钮
    - 用 setTimeout(loadAllShoes,0) 延后执行，避开 TDZ（脚本同步执行期 const 尚未初始化）
  - head 加 <link rel="prefetch" href="manifest.json">
  - loadMore 渲染模板：首屏前 8 张 eager + fetchpriority="high"，其余 lazy，统一 decoding="async"
- 结果：HTML 93KB → 56.4KB，内联 80 件 / 全量 336 件后台加载；node --check 语法校验通过
- 推送：与 github-actions bot 自动提交冲突 → checkout --ours 保留本地版 → push 6077b89，ls-remote 核实已同步
- 易错点：git status -sb 的 ahead/behind 因 tracking ref 陈旧会误报，以 git ls-remote 为准

## 2026-09-20 (续) — 图片 WebP 化 + 缩略图分层（首屏流量降 ~70%）

- 图片现状：336 张 / 20.6MB，最大边中位 726px、最大 1020px → 缩放没空间，只能靠格式与分层
- 步骤1 WebP 转换（系统 python 3.12 有 Pillow 12.2；managed 3.13 无 PIL）
  - 先用 os.link 硬链接备份到 images_original_backup/（零额外磁盘，已 gitignore）
  - quality=85/method=4 转换；若 webp ≥ 原图则删 webp 保留原图（避免负优化）
  - 结果：308 张待转 → 184 张转成功、124 张保留原图；20.6MB → 14.2MB（省 31%）
- 步骤2 缩略图分层（真正的大头）
  - make：thumbs/<分类>/<同名>.webp，宽 400px LANCZOS，q75 → 336 张共 4.00MB，平均 12KB（原图 43KB）
  - update_gallery.py 新增 thumb_path()，shoes 增加 thumb 字段；manifest 里 thumb 也转绝对 URL
  - index.html：新增 getShoeThumb(shoe)=shoe.thumb||getShoeImage，卡片用缩略图，弹窗 modalImage 仍用原图
  - LAZY_BLOCK 后台合并时 image 和 thumb 都剥掉绝对 URL 前缀
  - 首屏 40 张：~1.7MB → ~480KB（省约 72%）
- 修复 520aj 历史 bug：同一 script 内 `let filteredShoes` 重复声明 2 次（当年加分页时重复插入）→ 整个脚本 SyntaxError，520aj 实际是崩溃状态！已删除重复组，node --check 通过
- 易错点（重要）：转换/删除图片后**必须先 git add -A 让索引跟上**，否则 update_gallery.py 的 git 补录逻辑会把已删的旧 .jpg 路径当新图加进来（一度 336 → 548 件）
- 推送：主站 main 6ef8b85（含 thumbs 336 张入库）、520aj master d417684
